"""from math import sqrt
cateto_a = float(input('Digite o valor do cateto oposto: '))
cateto_b = float(input('Digite o valor do cateto adjacente: '))
hipotenusa = sqrt(cateto_a * cateto_a + cateto_b * cateto_b)
print('A soma do cateto oposto ( {:.0f} ) mais a soma do cateto adjacente ( {:.0f} )\nÉ igual a {:.2f} '
      'o comprimento da hipotenusa '.format(cateto_a, cateto_b, hipotenusa))"""

from math import hypot

co = float(input('Digite um o valor do Cateto Oposto: '))
ca = float(input('Digite o Valor do cateto Adjacente: '))
hi = hypot(co, ca)
print('O valor da hypotenusa é {:.02f}'.format(hi))
